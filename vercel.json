{
  "framework": "nextjs",
  "buildCommand": "cd apps/web && npx next build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "cd apps/web && npm install",
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.11@edge"
    }
  }
}
